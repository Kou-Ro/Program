#include <stdio.h>

void sort(char x[]);
char find_the_most_frequently_character(char x[]);

int main(void)
{
  char str[] = "chibakenkisarazushikiyomidaihigashi";
  char most_ch;

  printf("%s\n", str);
  
  /* (1) ここにアルファベット順のソートを書く */
  //sort(str);
  //printf("(1) %s\n", str);

  /* (2) ここに最も多く使用された文字を表示するプログラムを書く */
  //most_ch = find_the_most_frequently_character(str);
  //printf("(2) %c\n", most_ch);

  return 0;
}

void sort(char x[])
{

}

char find_the_most_frequently_character(char x[])
{
  
}
